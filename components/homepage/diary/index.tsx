'use client';

import { VerticalResizeHandle, Widget } from '@/components';
import classes from './diary.module.css';

export function Diary() {
    return <Widget className={classes.diary}>
        <div>this is diary</div>
           </Widget>;
}
